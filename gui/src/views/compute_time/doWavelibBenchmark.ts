const doWavelibBenchmark = async (o: {
  numSamples: number;
  waveletNames: string[];
}) => {
  const { numSamples, waveletNames } = o;
  return `placeholder for doWavelibBenchmark with numSamples=${numSamples} and waveletNames=${waveletNames.join(", ")}`;
};

export default doWavelibBenchmark;
